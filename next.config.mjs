/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Event infra — no fancy image optimization, no experimental features.
  poweredByHeader: false,
};

export default nextConfig;
